-- Global function. It is enough to import it only in main script and than it can be used everywhere
function print_separator()
	print("================================================================================");
end
